# Система динамической конвертации валют

## Обзор

Проект использует автоматическую конвертацию валют через API Национального Банка Республики Казахстан.

## ⚙️ ВАЖНО: Настройка курсов

### Если курсы неверные:

1. Перейдите в админ-панель → **💱 Currency** (в меню навигации)
2. Проверьте текущие курсы
3. Установите правильные курсы вручную
4. Или очистите кэш, чтобы загрузить курсы заново

### Актуальные курсы можно проверить:
- https://nationalbank.kz
- https://finance.kz
- https://kurs.kz

## Как это работает

### 1. Создание товара/картины
- При создании картины указывается **только цена в долларах ($)**
- Цены в тенге (₸) и евро (€) рассчитываются автоматически

### 2. Источник курсов валют
- **Основной API**: https://nationalbank.kz/rss/get_rates.php
- **Резервные курсы**: Если API недоступен, используются fallback значения (USD: 478.50, EUR: 520.80)
- Курсы обновляются ежедневно Нацбанком РК
- Данные кэшируются на 1 час в localStorage

### 3. Ручная установка курсов
В админ-панели есть раздел **💱 Currency Settings**, где можно:
- Просмотреть текущие курсы
- Установить курсы вручную
- Очистить кэш для принудительного обновления

**Пример установки:**
```
USD: 478.50
EUR: 520.80
```

Нажмите "Обновить курсы" - изменения применятся мгновенно.

### 3. Кэширование
- Курсы сохраняются в браузере на 1 час
- Это уменьшает количество запросов к API
- При истечении времени - автоматически обновляются

### 4. Fallback курсы
Если API недоступен, используются примерные курсы:
- 1 USD = 470 KZT
- 1 EUR = 510 KZT

## Файлы системы

### `/src/api/currency.ts`
Основной модуль конвертации валют:
- `getCurrencyRates()` - получить текущие курсы
- `convertUSDtoKZT(amount)` - конвертировать доллары в тенге
- `convertUSDtoEUR(amount)` - конвертировать доллары в евро
- `formatPrice(amount, currency)` - форматирование цены

### `/src/app/pages/admin/PaintingsManager.tsx`
- Автоматический расчет цен при вводе USD
- Отображение рассчитанных цен в реальном времени
- Показ текущих курсов валют

## Оформление заказов

### Процесс заказа
1. Пользователь добавляет товары в корзину
2. На странице checkout указывает email
3. Заказ сохраняется в backend/data/orders.json
4. Админ управляет статусами через админ-панель

### Статусы заказов
- **Ожидает (pending)** - новый заказ
- **В обработке (processing)** - заказ обрабатывается
- **Завершён (completed)** - заказ выполнен
- **Отменён (cancelled)** - заказ отменён

## Примеры использования

### В админ-панели
```
Указываете: $500
Автоматически рассчитывается:
- ₸ 235,000 (по курсу Нацбанка)
- € 456 (по курсу Нацбанка)
```

### На сайте
Цены отображаются во всех валютах с автоматическим обновлением по актуальному курсу.

## Обновление курсов

Курсы обновляются:
- При первой загрузке страницы
- Каждый час (автоматически)
- При создании/редактировании товара

## Техническая информация

### API Нацбанка
- **Формат**: XML
- **Частота обновлений**: ежедневно
- **Доступность**: публичный API, без ключа
- **Лимиты**: нет официальных лимитов

### Обработка ошибок
- При недоступности API используются fallback курсы
- Ошибки логируются в console
- Пользователь не видит технических ошибок

## Рекомендации

1. **Кэширование** работает автоматически, не требует настройки
2. **Fallback курсы** нужно обновлять вручную раз в несколько месяцев в `currency.ts`
3. **Мониторинг** - периодически проверяйте актуальность курсов в админ-панели
